// // SSO_server/script.js

// document.addEventListener('DOMContentLoaded', () => {

//     // 1. Danh sách các class nền bạn vừa tạo trong CSS
//     const backgroundClasses = ['bg-nen-1', 'bg-nen-2'];

//     // 2. Chọn ngẫu nhiên một số (0 hoặc 1)
//     const randomIndex = Math.floor(Math.random() * backgroundClasses.length);

//     // 3. Lấy tên class ngẫu nhiên từ danh sách
//     const randomClass = backgroundClasses[randomIndex];

//     // 4. Thêm class đó vào thẻ <body> của trang
//     document.body.classList.add(randomClass);
// }); 